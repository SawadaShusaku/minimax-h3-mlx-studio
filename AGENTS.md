# MiniMax-H3 生成設定ガイド

このファイルは設定を固定する規則ではない。コンテと素材から適切な設定を選び、同じ設定ミスを繰り返さないための判断基準である。根拠と実例は `docs/h3-settings-research.md` を参照する。

## 先にコンテから読み取ること

設定前に、次の3点を明確にする。

1. 映像の種類と必要な精細さ：実写の肌・衣服・商品・文字など、細部が評価対象か。
2. 被写体とカメラの運動量：静かな寄りか、口・手・群衆・高速パンなどを含むか。
3. 必要尺とカット構成：1生成で完結するか、複数ショットを編集でつなぐか。

人物・衣装・画風・声の継続が必要かは、コンテと参照素材から判断する。コンテ上で明らかな事項を、一般論としてユーザーへ聞き直さない。

## 変えないモデル条件

- H3の公式生成範囲は4〜15秒、24fps。フレーム数は `17k+5`、解像度の各辺は32の倍数にする。このアプリが選択肢として出す検証済み範囲は124〜362 frames（約5〜15秒）。
- 標準のネイティブ解像度は短辺768。16:9なら `1344x768`、9:16なら `768x1344` が基準になる。低解像度生成は構図確認用であり、最終画質の判定には使わない。
- FL2VAとRef2VAは別チェックポイント。FL2VAはテキスト／始終端フレーム、Ref2VAは参照画像・映像・音声を扱う。
- このアプリではRef2VAとキーフレーム／チェインウィンドウを併用できない。15秒を超える映像は、原則として15秒以内のショットを個別生成して編集でつなぐ。
- Ref2VAでは各素材を `<Picture 1>`、`<Video 1>`、`<Audio 1>` と指定し、identity、style、motion、camera、voiceのどれを担うかプロンプトで明記する。
- `ref_image_size=match` は生成面積に合わせる通常設定。元画像の細部や同一性が特に重要で、時間・メモリ増を許容できる場合だけ `max` を使う。

## ステップとTurboの選び方

最初に、チェックポイント、Turbo LoRAの名称・対象タスク・学習解像度・推奨ステップを特定する。Turbo LoRAごとに蒸留条件が違うため、「Turboなら常に4/6/8 steps」と一括りにしない。

- Base（Turboなし）：公式ComfyUIテンプレートの出発点は20 steps。通常のショットは20、実写の肌や細部、激しい動き、口・手・音声を重視するショットは25〜32を検討する。MLX ServeのBase既定は30。
- 公式LightX2V Turbo：各ファイルの指定値を使う。現行配布にはFL2VA 4-step、FL2VA 8-step、Ref2VA 4-stepがあり、シフト値もLoRAと組で合わせる。Ref2VA版は `match`、544p系、video/audio shift 12/3、4 NFEが配布元の一組である。
- このプロジェクトのFL2VAパックで現在有効なのはLarry `v4 step600 EMA` Turbo。配布元は4〜8を有効範囲、6〜8を4より良好としている。6は速度側、最終出力や動き・細部を重視する場合は8から選ぶ。旧 `v1 ckpt850 EMA` は版名付きで退避してあり、既定へ戻さない。
- Turboの強度は、配布元が別値を指定しない限り1.0から始める。

現在のmainにあるRef2VAパックには `turbo_lora.safetensors` がない。`turbo=true` は失敗するため、Ref2VA対応が配布元で確認できるLoRAを追加するまではBaseで生成する。FL2VAパック内のLoRAを、互換性の確認なしにRef2VAへリンクしない。Ref2VA専用4-step版も実運用報告では画質評価が割れているため、導入後もBase既定を自動でTurboへ変えず、明示的な速度優先時だけ選ぶ。

2026-08-16のローカル確認では、全面モザイクのある旧Hilltopと同じ既知条件（1024x768、124f、6 steps、seed 11）をv4で再生成すると、大きなブロック状模様が大幅に改善した。圧縮前PNGとMP4がほぼ同じため、この例ではH.264を主因としない。ただし旧生成はTurbo LoRAの実ファイルと `fast` が履歴未記録なので、完全な一変数比較とは表現しない。

## 速度機能を選ぶ基準

- ローカルの `fast=true` はBase時にstep cacheとattention broadcastを使う近似処理で、同じseedでも動きや細部が変わり得る。
- 激しい動き、顔・肌・衣服・背景の微細構造、時間的一貫性が評価対象なら、Baseの `fast=false` を検討する。静かな構図確認などでは `fast=true` を選べる。
- 現行MLX ServeはTurbo時に上記キャッシュを自動停止する。Turbo出力の劣化をキャッシュとの重ね掛けのせいにしない。
- このアプリはsampler、scheduler、video/audio shiftを露出していない。ComfyUIの `euler/beta` 等を、そのまま選択可能なローカル設定として記述しない。

## 長尺と一貫性

- 1回の自然な演技・台詞・カメラ運動は、可能ならモデルの範囲内で最長15秒にまとめる。細かく分割するほど映像と音声の接合点が増える。
- 別カット間の人物・衣装・画風・声は、同じRef素材と明示的な役割指定で固定する。同じプロンプトの使い回しだけでは、同じ行動を再演する可能性がある。
- ショットごとに、時間帯、画角、カメラ位置、開始状態、行動、終了状態、台詞の範囲を更新する。Refは一貫性の条件、プロンプトはそのカットで起こす変化を担当する。
- FL2VAの最終フレーム連結は動きの入口を渡せるが、背景・人物・音声の長尺一貫性を保証しない。Ref2VAの代替として扱わない。

## 実行前に残す設定

履歴には、モード／チェックポイント、LoRAの正確なファイル名、Turbo、steps、fast、解像度、フレーム数、seed、Ref素材と `ref_image_size` を残す。Turbo使用時は `turbo_lora` にモデルパック、設定パス、リンク解決先、実ファイル名、サイズ、SHA-256を自動記録する。旧履歴のLoRA版は推定で補完しない。画質を評価するときは「粗い」だけでなく、肌、輪郭、背景、動き、口、音声のどこが崩れたかを記録する。

動画の方針、コンテ、使用素材、主要設定が決まったら、生成を始める前に、過去の実測値と今回の解像度・フレーム数・steps・生成本数から所要時間を見積もる。ユーザーへ所要時間の幅と推定完了時刻を知らせ、生成を開始してよいか確認する。設定や本数が変わった場合は見積もりも更新する。
